import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ashfall.undeadafterdark',
  appName: 'Undead Afterdark',
  webDir: 'www',
  backgroundColor: '#050505',
  ios: {
    scheme: 'UndeadAfterdark',
    backgroundColor: '#050505'
  }
};

export default config;
